# CoM_Importer

A Python project scaffold for importing and processing CoM data.

## Quick Start

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -e .[dev]
pytest
python -m com_importer
```
