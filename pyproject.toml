[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "com-importer"
version = "0.1.0"
description = "Importer utilities for CoM data"
readme = "README.md"
requires-python = ">=3.10"
authors = [
  { name = "Mythic" }
]
dependencies = []

[project.optional-dependencies]
dev = [
  "pytest>=8.0"
]

[tool.pytest.ini_options]
python# CoM_Importer

A Python project scaffold for importing and processing s/
A Python proImp
## Quick Start

```bash
python3 -m venv .venv
source .venv/binkag
```bash
pyth_ =pythonn"source .venv/bin/act/mpip install -e .[dev]
py/Cpytest
python -m comimpythor/```
